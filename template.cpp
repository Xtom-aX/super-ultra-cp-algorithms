#include <bits/stdc++.h>

using namespace std;

#pragma region Define_y
// Typy pozwalające przechowywać większe wartości
#define int long long
#define float long double

// Skrótowa pętla
#define FOR(i, l, r) for (int i = (l); i < (r); i++)
#define RFOR(i, l, r) for (int i = (r) - 1; i >= (l); i--)

#define s(x) ((int)(x).size())
#define iter(v) (v).begin(), (v).end()

#define pii pair<int, int>

#define st first
#define nd second

#define DEBUG

#ifdef DEBUG
#define dbg(x) cerr << #x << " = " << (x) << "\n";
#else
#define dbg(x)
#endif

#pragma endregion


signed main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    // Tutaj solve

    return 0;
}
